---
# Legacy macOS pre-checks (minimal stub for syntax-check)
- name: Display macOS pre-checks banner
  ansible.builtin.debug:
    msg: |
      === macOS Pre-Checks (Legacy) ===
      Host: {{ inventory_hostname }} ({{ ansible_host | default('n/a') }})
      User: {{ ansible_user | default('n/a') }}
  changed_when: false
