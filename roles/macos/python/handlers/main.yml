---
#SPDX-License-Identifier: MIT-0
# handlers file for roles/macos/python

- name: Refresh UV environment
  ansible.builtin.shell: |
    set -o pipefail
    export PATH="$HOME/.local/bin:$PATH"
    uv --version || echo "UV not installed"
  args:
    executable: /bin/bash
  changed_when: false
