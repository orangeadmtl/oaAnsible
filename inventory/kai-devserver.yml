---
all:
  vars:
    # Ansible configuration
    ansible_python_interpreter: "/usr/bin/python3"
    ansible_ssh_common_args: "-o StrictHostKeyChecking=no"
    
    # Platform configuration
    platform_type: "debian"
    
    # Shell enhancement with shell_manager role
    shell_manager_enabled: true
    # Use defaults from role, no custom overrides needed

  children:
    kai_devserver:
      hosts:
        kai-devserver:
          ansible_host: "100.77.244.14"
          ansible_user: kai
          ansible_become_password: "{{ vault_sudo_passwords['kai-devserver'] }}"
          ansible_port: 22